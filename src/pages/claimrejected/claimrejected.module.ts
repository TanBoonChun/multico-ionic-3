import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ClaimrejectedPage } from './claimrejected';

@NgModule({
  declarations: [
    ClaimrejectedPage,
  ],
  imports: [
    IonicPageModule.forChild(ClaimrejectedPage),
  ],
})
export class ClaimrejectedPageModule {}
