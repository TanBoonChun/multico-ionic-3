import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ClaimapprovedPage } from './claimapproved';

@NgModule({
  declarations: [
    ClaimapprovedPage,
  ],
  imports: [
    IonicPageModule.forChild(ClaimapprovedPage),
  ],
})
export class ClaimapprovedPageModule {}
